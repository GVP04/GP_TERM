Переменный в сообщениях доделать.

Можешь cделать OnTimerEx, чтобы возвращалось считалось от 1 до последнего (или до бесконечности).То есть, если я пишу ONTIMER,3,1000,100 будет приходить
... 3^1
... 3^2
...
... 3^100

если я пишу ONTIMER,5,1000,0 (или отрицательное) будет приходить
... 5^1
... 5^2
...
... 5^... т.д.



CHAR(2) позволяет добавить методы к событию, но только один раз. Если я в программе удаляю событие, а потом добавляю его с тем же синтаксисом, что и в первый раз, то приходит только само событие - методы не выполняются.


SetIgnoreSelChangeOnTAB работает, но иногда сбивается. К тому же нужно игнорировать не только по TAB, но и по NextDlgCtrl и PrevDlgCtrl тоже. Они по сути делают ту же работу. TAB равен NextDlgCtrl, а Shift-TAB равен PrevDlgCtrl.


Можно ли запоминать в EDIT последний SetText c тем, чтобы впоследстии с помощью некоего метода, например, GetChange, можно было получить список номеров объектов в которых текст изменился? Можно и их данные тоже.



По поводу OnValChange для EDIT. Разрешенными событиями являются только действия по редактированию (как клавишами, так и мышью). Любое другое событие должно порождать OnValChange в случае, если данные изменились с момента SetFocus.


Можно ли сделать режим для EDIT, при котором можно только добавлять символы в конец строки и удалять последний символ с помощью Backspace? Выделение соответственно не должно работать.


Можно ли сделать режим для EDIT, при котором стрелки вверх и вниз работали бы как TAB и Shift-TAB?


Это громоздко. Я скорее всего не буду фильровать вообще. Раз пошли какие-то действия, то надо закончить ввод.


Нужно на момент OnSetFocus запоминать содержимое EDIT, а затем при любом действии с любым другим объектом или окном проверять текущее значение и, если оно изменилось, слать OnValChange.

Нужно событие OnValсhange, которое должно происходить в EDIT при выходе из него при условии, что данные изменились по сравнении с тем, какими они были на момент OnSetFocus. Событие должно приносить данные. Под выходом я понимаю любое действие пользователя над другим объектов, в том числе OnClose и т.п.


Назначаю методы сообщению (с пом. CHAR(2)), потом удаляю сообщение, потом назначаю опять и доп. данные уже не приходят. Нужно, чтобы приходили, а в идеале, чтобы можно было повторно не прописывать все это. Но это наверно приведет к тому, что нужно будет новое имя назначать такому "рукотворному" событию.

Нужно событие OnKeyPress, которое присылает десятичный код клавиши, не нарушая встроенную функциональность объектов. То есть, стрелка вниз и т.п. в LISTCTRL должна работать, и при этом  должно приходить сообщение OnKeyPress.

Нужен режим, при котором события посылают сообщения только в случае, если это действие пользователя. SetWindowText, например, не должен вызывать события и т.п.

Нужно событие OnValсhange, которое должно происходить в EDIT при выходе из него при условии, что данные изменились по сравнении с тем, какими они были на момент OnSetFocus. Событие должно приносить данные. Под выходом я понимаю любое действие пользователя над другим объектов, в том числе OnClose и т.п.

Можешь сделать так, чтобы briz сам подгружал список, если он запрашивается, а в пямяти его нет?

Остается проблема - самопроизвольно добавляются чужие изображения в мой список.

У меня не работает копирование в экране терминала (Ctrl - мышь). Проверь, пожалуйста.

У меня есть FULLROWSELECT в стилях наряду с LIST, но выделяется только текст

Выполняю метод 'SetIgnoreSelChangeOnTAB 1' - все равно выделяется.

